[include]
path = ~/.gitconfig.local

[alias]
tags = tag -l
branches = branch --all
remotes = remote --verbose
graph = log --graph --date-order --all --pretty=format:'%h %Cred%d %Cgreen%ad %Cblue%cn %Creset%s' --date=short
ic = commit --allow-empty --signoff --message 'Initial commit'

[fetch]
parallel = 48

[user]
email = sora@morimoto.io
name = Sora Morimoto

[sendemail]
smtpuser = sora@morimoto.io
smtpserver = smtp.fastmail.com
smtpencryption = tls
smtpserverport = 587
suppresscc = self

[submodule]
fetchJobs = 48

[core]
autocrlf = false
eol = lf
pager = delta --theme base16
precomposeunicode = false
trustctime = false
untrackedCache = true
whitespace = space-before-tab,-indent-with-non-tab,trailing-space

[interactive]
diffFilter = delta --color-only

[protocol]
version = 2

[color]
ui = auto

[apply]
whitespace = fix

[pack]
useSparse = true

[push]
default = simple
followTags = true

[pull]
rebase = true

[merge]
log = true

[rebase]
autosquash = true

[url "git@github.com:"]
insteadOf = https://github.com

[ghq]
root = ~/src
